# 공개 값만 EXPO_PUBLIC_ 접두어 (빌드 시 번들 inline). 시크릿은 금지 → EAS env(C-3).
# 값은 constants/env.ts(Env)에서 타입·검증을 거쳐 소비한다.

# development | preview | production. 미설정 시 __DEV__ 기준으로 development/production 자동 결정.
# EXPO_PUBLIC_APP_ENV=development

# EXPO_PUBLIC_API_BASE_URL=
