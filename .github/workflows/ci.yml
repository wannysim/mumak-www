name: CI

on:
  pull_request:
    # Keep PR CI unfiltered so the `CI Success` summary check is always
    # created when branch protection requires it. Non-app changes are skipped
    # inside the workflow by detect-scopes.
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: [main, develop]
    paths:
      - "apps/**"
      - "packages/**"
      - "scripts/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "turbo.json"
      - "**/.oxlintrc.json"
      - ".oxfmtrc.jsonc"
      - ".nvmrc"
      - ".github/workflows/**"
      - ".github/actions/**"
      - ".github/app-config/**"
  workflow_dispatch:
    inputs:
      scopes:
        description: "Comma-separated list of app names (optional, auto-detected if not provided)"
        required: false
        type: string

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  TURBO_TEAM: ${{ vars.TURBO_TEAM }}
  TURBO_REMOTE_CACHE_SIGNATURE_KEY: ${{ secrets.TURBO_REMOTE_CACHE_SIGNATURE_KEY }}

jobs:
  # Step 1: Detect which apps have changed
  detect-scopes:
    name: Detect Changed Apps
    runs-on: ubuntu-latest
    outputs:
      scopes: ${{ steps.detect.outputs.scopes }}
      has_changes: ${{ steps.detect.outputs.has_changes }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Get changed files
        id: changed-files
        if: github.event_name == 'pull_request'
        uses: tj-actions/changed-files@v47

      - name: Detect changed apps
        id: detect
        uses: ./.github/actions/detect-scopes
        with:
          manual_scopes: ${{ inputs.scopes }}
          changed_files: ${{ steps.changed-files.outputs.all_changed_files }}

  # Step 2: Prepare matrix from detected scopes
  prepare-matrix:
    name: Prepare Matrix
    needs: detect-scopes
    if: needs.detect-scopes.outputs.has_changes == 'true'
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.convert.outputs.matrix }}
      count: ${{ steps.convert.outputs.count }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Convert scopes to matrix
        id: convert
        uses: ./.github/actions/scopes-to-matrix
        with:
          scopes: ${{ needs.detect-scopes.outputs.scopes }}

  # Step 3: Warm the pnpm store cache once before any matrix job runs.
  # Parallel matrix jobs using the same pnpm-lock hash would otherwise race on
  # `actions/cache/save` and surface "Unable to reserve cache" warnings. Keeping
  # a single writer here lets all downstream jobs restore-only.
  warm-pnpm-cache:
    name: Warm pnpm cache
    needs: [detect-scopes]
    if: needs.detect-scopes.outputs.has_changes == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js and pnpm (save cache)
        uses: ./.github/actions/setup-node-pnpm
        with:
          save-cache: "true"

  # Step 4: Lint/format root-only files (scripts/, .cursor/, .ai/)
  # Runs in parallel with the per-app validate matrix.
  root-quality:
    name: Quality (root only)
    needs: [detect-scopes, warm-pnpm-cache]
    if: needs.detect-scopes.outputs.has_changes == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js and pnpm
        uses: ./.github/actions/setup-node-pnpm
        with:
          save-cache: "false"

      - name: Restore Turbo cache
        uses: actions/cache@v6
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-root-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-root-
            ${{ runner.os }}-turbo-

      - name: Lint (root files)
        run: pnpm turbo run lint:root

      - name: Format Check (root files)
        run: pnpm turbo run format:root:check

  # Step 5: Validate each app — lint, format, types, test, build (parallel matrix)
  validate:
    name: Validate (${{ matrix.app }})
    needs: [detect-scopes, prepare-matrix, warm-pnpm-cache]
    if: needs.detect-scopes.outputs.has_changes == 'true' && needs.prepare-matrix.outputs.count != '0'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        app: ${{ fromJson(needs.prepare-matrix.outputs.matrix) }}
    env:
      # blog는 과거 standalone 프리렌더 산출물 중복으로 remote 업로드가 413이었다(#384).
      # turbo.json `blog#build` outputs에서 top-level `.next/server`(standalone 복제본의 원본)를
      # 빼 산출물을 절반 가까이 줄였으므로(117.8→65.3MB gz), remote read/write를 다시 켠다.
      # 413이 재발하면 turbo는 업로드 실패를 경고로만 남기고 task는 성공하므로 CI는 깨지지 않는다.
      TURBO_CACHE_ARG: ""
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Node.js and pnpm
        uses: ./.github/actions/setup-node-pnpm
        with:
          save-cache: "false"

      - name: Restore Turbo cache
        uses: actions/cache@v6
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ matrix.app }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-${{ matrix.app }}-
            ${{ runner.os }}-turbo-

      - name: Lint
        run: pnpm turbo run lint --filter=${{ matrix.app }}

      - name: Format Check
        run: pnpm turbo run format:check --filter=${{ matrix.app }}

      # check-types/test:ci/build는 모두 build에 dependsOn하므로, blog에 한해
      # 동일한 $TURBO_CACHE_ARG를 일관되게 넘겨 같은 task hash를 재사용하고
      # remote write만 억제한다(#403).
      - name: Type Check
        run: pnpm turbo run check-types --filter=${{ matrix.app }} $TURBO_CACHE_ARG

      - name: Test
        run: pnpm turbo run test:ci --filter=${{ matrix.app }} $TURBO_CACHE_ARG

      - name: Validate Content (blog only)
        if: matrix.app == 'blog'
        run: |
          pnpm turbo run validate:content --filter=blog
          pnpm turbo run validate:garden --filter=blog
          pnpm turbo run validate:design --filter=blog

      - name: Build
        run: pnpm turbo run build --filter=${{ matrix.app }} $TURBO_CACHE_ARG

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-${{ matrix.app }}
          path: apps/${{ matrix.app }}/coverage/
          if-no-files-found: ignore
          retention-days: 7

      - name: Append coverage summary
        if: always()
        run: |
          summary_file="apps/${{ matrix.app }}/coverage/coverage-summary.json"
          if [ ! -f "$summary_file" ]; then
            echo "### ${{ matrix.app }} — coverage summary unavailable" >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          {
            echo "### ${{ matrix.app }} — coverage"
            echo ""
            echo "| Metric | Coverage |"
            echo "| --- | ---: |"
            jq -r '
              .total |
              [
                "| Statements | \(.statements.pct)% (\(.statements.covered)/\(.statements.total)) |",
                "| Branches   | \(.branches.pct)% (\(.branches.covered)/\(.branches.total)) |",
                "| Functions  | \(.functions.pct)% (\(.functions.covered)/\(.functions.total)) |",
                "| Lines      | \(.lines.pct)% (\(.lines.covered)/\(.lines.total)) |"
              ] | .[]
            ' "$summary_file"
            echo ""
          } >> "$GITHUB_STEP_SUMMARY"

  # Step 6: Aggregate coverage from validate artifacts and upload to Codecov
  # Avoids the previous duplicate test:ci execution.
  coverage:
    name: Upload Coverage
    needs: [detect-scopes, validate]
    if: needs.detect-scopes.outputs.has_changes == 'true' && needs.validate.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Download coverage artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: coverage-*
          path: coverage-artifacts

      # 앱별로 flag를 분리해 업로드해야 codecov 코멘트와 status가 앱 단위로 분리된다.
      # action 자체는 incremental upload를 지원해 여러 step으로 안전하게 호출 가능.
      - name: Upload blog coverage to Codecov
        if: hashFiles('coverage-artifacts/coverage-blog/lcov.info') != ''
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage-artifacts/coverage-blog/lcov.info
          flags: blog
          fail_ci_if_error: false

      - name: Upload mumak-next coverage to Codecov
        if: hashFiles('coverage-artifacts/coverage-mumak-next/lcov.info') != ''
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage-artifacts/coverage-mumak-next/lcov.info
          flags: mumak-next
          fail_ci_if_error: false

      - name: Upload mumak-react coverage to Codecov
        if: hashFiles('coverage-artifacts/coverage-mumak-react/lcov.info') != ''
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage-artifacts/coverage-mumak-react/lcov.info
          flags: mumak-react
          fail_ci_if_error: false

      - name: Upload mumak-native coverage to Codecov
        if: hashFiles('coverage-artifacts/coverage-mumak-native/lcov.info') != ''
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage-artifacts/coverage-mumak-native/lcov.info
          flags: mumak-native
          fail_ci_if_error: false

      - name: Upload lattice coverage to Codecov
        if: hashFiles('coverage-artifacts/coverage-lattice/lcov.info') != ''
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage-artifacts/coverage-lattice/lcov.info
          flags: lattice
          fail_ci_if_error: false

      - name: Upload karaoke coverage to Codecov
        if: hashFiles('coverage-artifacts/coverage-karaoke/lcov.info') != ''
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage-artifacts/coverage-karaoke/lcov.info
          flags: karaoke
          fail_ci_if_error: false

  # Summary job for branch protection. `always()` survives concurrency
  # cancellation, so use `!cancelled()` while still aggregating failed needs.
  # Source: https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-cancellation
  ci-success:
    name: CI Success
    needs: [detect-scopes, prepare-matrix, warm-pnpm-cache, root-quality, validate]
    if: ${{ !cancelled() }}
    runs-on: ubuntu-latest
    steps:
      - name: Check results
        run: |
          if [ "${{ needs.detect-scopes.result }}" != "success" ]; then
            echo "detect-scopes did not succeed (result: ${{ needs.detect-scopes.result }})"
            exit 1
          fi

          if [ "${{ needs.detect-scopes.outputs.has_changes }}" != "true" ]; then
            echo "No relevant changes detected - CI passed"
            exit 0
          fi

          if [ "${{ needs.prepare-matrix.result }}" != "success" ] ||
             [ "${{ needs.warm-pnpm-cache.result }}" != "success" ] ||
             [ "${{ needs.root-quality.result }}" != "success" ]; then
            echo "CI setup or root quality failed"
            exit 1
          fi

          if [ "${{ needs.prepare-matrix.outputs.count }}" == "0" ] &&
             [ "${{ needs.validate.result }}" == "skipped" ]; then
            echo "No apps to validate - CI passed"
            exit 0
          fi

          if [ "${{ needs.validate.result }}" != "success" ]; then
            echo "Some validations failed"
            exit 1
          fi

          echo "All validations passed"
