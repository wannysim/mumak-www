# 이미지 업로드 시스템 — 요구사항 정리와 설계 근거

> 2026-08-08 확정. 요약본은 `plan.md`의 "Phase 4" 섹션, 이 문서는 요구사항 해석과 결정 근거의 전문(全文).

## 1. 요구사항

### 1-1. 명시적 요구

| 요구       | 내용                                                                                    |
| ---------- | --------------------------------------------------------------------------------------- |
| 목적       | 홈서버 기반 이미지 업로드 시스템. Vercel 탈출 프로젝트의 처음부터 선언된 엔드게임       |
| 사용처 1   | blog — 글에 들어가는 이미지                                                             |
| 사용처 2   | (미래) photo app — 사진가로서의 포트폴리오 사이트                                       |
| 관리       | 여러 앱의 이미지를 한 곳에서 관리하는 "통합 admin"                                      |
| 규모       | 전시용 JPEG, 최대 4096px·장당 ~10MB, 총량 상한 ~100GB. RAW/원본은 기존 보관처 유지      |
| 저장 위치  | 홈서버 (Cloudflare R2 등 외부 서비스 대신 — 데이터 주권)                                |
| admin 접근 | LAN/WireGuard 전용 ("admin은 Cloudflare 미노출" 기존 결정의 연장)                       |
| 자원 제약  | 홈서버 RAM 4GB를 12개 컨테이너가 공유 → 컨테이너별 메모리 상한 필수. 디스크 ~300GB 여유 |

### 1-2. 도출된 요구 (설계가 반드시 충족해야 하는 암묵적 조건)

**(a) 재빌드 없는 이미지 발행 — 사실상 숨은 1순위.**
블로그는 완전 SSG(922 라우트)이고 이미지가 `content/`·`public/`에 들어가 Docker 이미지
레이어에 구워진다. 즉 사진 한 장 추가 = 전체 재빌드 + 재배포이며, 배포 순간 ~10초 다운
(2026-08-08 원인 확정된 watchtower 교체 blip)이 사진 한 장에도 발생한다. 포트폴리오는
이미지가 콘텐츠 본체라 업로드 빈도가 글보다 높다 — 이 구조로는 운영 불가. 제1 원칙:
**이미지를 빌드 산출물에서 분리해 URL 참조로 바꾼다.**

**(b) 가정용 회선의 업로드 대역폭 보호.**
홈서버 회선은 업로드(방문자에게 서빙하는 방향)가 좁고, 이미지는 트래픽 바이트의 대부분을
차지한다. Cloudflare가 이미지의 사실상 원천 서버가 되도록 캐시 적중률을 설계 단계에서
보장해야 한다. 나중에 붙이는 최적화가 아니라 홈서버 서빙의 전제 조건.

**(c) 운영자 1인 — 관리 표면 최소화.**
새 시스템 = 새 관리 표면. 기존 검증 패턴(Portainer 스택, NPM 프록시, Cloudflare orange,
Vaultwarden, mem_limit + 로그 로테이션)을 복제하고, admin UI는 저장소 내장품을 쓴다.
커스텀 코드 0에서 시작.

**(d) Vercel 프리뷰 호환.**
about/now DB 전환을 보류시킨 결정적 사유가 "프리뷰가 홈서버 LAN의 postgres에 접근 불가"
였다. 이미지는 공개 URL로 서빙되는 순간 프리뷰·프로덕션·로컬 dev가 같은 URL을 그냥 본다.
이미지 시스템이 DB보다 먼저 와야 하는 이유.

**(e) 데이터의 대체 가능성이 백업 요구를 낮춘다.**
홈서버의 JPEG은 전부 원본(별도 보관)에서 재생성 가능한 파생물. 디스크가 죽어도 잃는 것은
재업로드하는 수고뿐 → 백업은 필수 인프라가 아니라 선택적 편의.

## 2. 설계

### 2-1. 아키텍처

```
[업로드 — LAN/WireGuard에서만]
  나 → MinIO Console (http://10.0.0.105:9101) → 버킷 blog / photo

[저장 — 인터넷 완전 미노출]
  MinIO (:9100, S3 API) — 홈서버 디스크의 minio_data 볼륨

[서빙 — 유일한 공개 진입점]
  방문자 → Cloudflare(orange, 캐시) → NPM → imgproxy(:8061) → (S3 readonly) → MinIO

[참조]
  blog MDX·photo app: https://img.wannysim.com/<preset>/plain/s3://blog/2026/foo.jpg
```

### 2-2. 컴포넌트별 결정과 근거

**저장소: MinIO — "S3 프로토콜을 말하는 홈서버 디스크"**

- 검토 대안: ① Docker 볼륨 + 직접 짠 업로드 API — 가장 작아 보이지만 업로드 UI·인증·
  목록·삭제를 전부 직접 짜야 해서 실제로는 가장 크다. ② Cloudflare R2 — 운영 0에 egress
  무료로 객관적으로 매력적이나, "홈서버 기반" 엔드게임 선언과 충돌(사용자 직접 기각).
  ③ MinIO — 단일 컨테이너로 S3 API + 웹 Console + 접근키 관리.
- 결정적 이유는 **S3 API 생태계 재사용**: imgproxy가 S3 소스를 네이티브로 읽고, 미래
  photo app도 검증된 S3 SDK를 endpoint만 바꿔 그대로 쓴다. AWS와는 무관 — 데이터는
  홈서버 디스크에, 과금 없음.
- **버킷 = 앱 경계** (`blog`/`photo`): 통합 관리가 폴더 수준이 아니라 권한·정책 수준에서 분리.
- **인터넷 완전 미노출**: NPM·Cloudflare에 미등록. 관리 표면이 실수로 공개되는 사고
  (proxy.5231.kr 노출 발견 사례)가 구조적으로 불가능.

**"통합 admin" = MinIO Console (커스텀 admin 0줄)**

- Console이 업로드(드래그앤드롭)·브라우징·삭제·버킷별 관리를 내장 제공 — 요구의 정확한 답.
- 커스텀 admin이 정당해지는 시점은 파일이 아니라 **메타데이터**(앨범·순서·태그·EXIF 노출)를
  관리할 때 = photo app 착수 시점. 그때 postgres에 메타데이터를 두고 admin의 위치(photo app
  내장 vs 별도 앱)를 재결정한다. 지금 만들면 요구 확정 전의 추측 코드.

**변환·서빙: imgproxy — 사전 변환 대신 on-the-fly**

- 사전 변환(업로드 시 썸네일·webp 생성)은 업로드 파이프라인(큐·워커·실패 처리)이 필요하고
  새 사이즈 추가 시 전체 재처리가 필요하다. on-the-fly는 첫 요청에만 변환 비용을 내고
  이후는 Cloudflare 캐시가 받는다. 트래픽이 캐시로 흡수되는 개인 사이트에선 압도적으로 단순.
- **서명 URL 대신 프리셋 고정** (`IMGPROXY_ONLY_PRESETS=true`, `content`/`thumb`/`full`
  2~3개): MDX를 손으로 쓰는 워크플로에서 서명 URL은 사용 불가. 프리셋 고정은 사람이 쓸 수
  있는 URL을 유지하면서 리사이즈 증폭 DoS(임의 사이즈 변환으로 CPU 소모)를 차단한다.
  트레이드오프: **프리셋 목록 = 공개 URL 계약** — 변경은 추가만, 삭제·의미 변경은 발행된
  글의 URL을 깨뜨리므로 금지.
- `IMGPROXY_AUTO_WEBP/AVIF`로 브라우저별 포맷 자동 협상. 4096px 소스의 디코딩 스파이크가
  있으므로 동시 처리 제한 + `mem_limit 256m`.

**Cloudflare 캐시 전략 — (b)의 답**

- imgproxy 응답에 장기 Cache-Control + **이미지 키 불변(immutable) 운용**: 덮어쓰기 금지,
  갱신 = 새 키(날짜/버전 포함 파일명) + MDX 링크 교체. 캐시 무효화라는 문제 자체가 사라지고
  Cloudflare 엣지가 사실상 이미지 CDN 원천이 된다. 홈서버는 캐시 미스 때만 일한다.

**blog 통합 — 코드 변경은 의도적으로 소폭**

- `next.config.mjs` `images.remotePatterns`에 `img.wannysim.com` 추가.
- next/image 옵티마이저가 imgproxy 결과물을 재최적화하면 같은 이미지를 두 번 처리(blog
  컨테이너 sharp + imgproxy) — MDX `img` 오버라이드에서 이 호스트는 unoptimized로 우회해
  "최적화는 imgproxy 한 곳" 원칙 고정.
- 발행 워크플로: Console 드래그 → URL 복사 → MDX 붙여넣기. 글 발행(재빌드)과 이미지가 완전 분리.

**자원 예산**

| 컨테이너 | mem_limit | 실사용 예상                      |
| -------- | --------- | -------------------------------- |
| MinIO    | 512m      | ~150–250MB                       |
| imgproxy | 256m      | 유휴 ~60MB, 변환 스파이크 ~200MB |

- blog 하드닝과 같은 원칙: 폭주 시 이웃(NPM/AdGuard) 대신 자기만 죽고 재시작.
- 디스크 상한 ~100GB vs 여유 ~300GB. 로그 로테이션 전 스택 공통.

**백업 — (e)의 결론**

- 필수 아님. 복구 전략 = 원본에서 재업로드. 원하면 야간 rclone → R2(~100GB는 무료 티어
  초과, 월 ~$1.5). MinIO 가동 후 결정.

**업데이트 정책**

- watchtower는 `mumak-blog`만 감시 → MinIO/imgproxy는 자동 업데이트 안 됨(의도).
  저장소 소프트웨어의 무인 메이저 업데이트는 리스크 — 릴리즈 태그 고정 + 수동 업데이트.

### 2-3. 리스크와 한계

1. **홈서버 SPOF는 여전** — 캐시 미스 상태의 이미지는 홈서버 다운 시 안 나온다. 블로그
   본문(SSG)과 운명이 같으므로 새 리스크가 아니라 기존 리스크에 편승.
2. **MinIO 단일 노드·단일 디스크** — RAID·이중화 아님. (e) 덕분에 수용 가능 판정.
3. **프리셋 = 깨면 안 되는 공개 계약** — 초기 이름 설계가 중요.
4. **RAM 예상치는 일반 수치** — 이 박스 실측 아님. 배포 직후 Portainer 컨테이너 통계로
   실사용 확인 후 상한 조정.

### 2-4. 구축 순서

1. ~~사전 실측~~ → 디스크 ~300GB 확인. RAM은 배포 후 Portainer 통계로 검증
2. MinIO 스택(9100/9101) → 버킷 `blog`·`photo` + imgproxy용 readonly 접근키
3. imgproxy 스택(8061) + 프리셋 정의
4. 공개 경로: Cloudflare A `img`(orange) + NPM 프록시 호스트(와일드카드 Origin CA 재사용)
5. 검증: 업로드 → 프리셋 URL 200 → webp 협상 → 2회째 `cf-cache-status: HIT`
6. blog 코드 PR: remotePatterns + mdx img 우회 + 업로드 규약 `apps/blog/AGENTS.md` 문서화
7. (후속) 백업 결정. photo app 착수 시 메타데이터·admin 재평가
